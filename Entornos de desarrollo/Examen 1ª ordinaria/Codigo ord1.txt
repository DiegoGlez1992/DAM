package javaapplication7; import java.util.ArrayList; import java.util.InputMismatchException; import java.util.List; import java.util.Scanner; public class JavaApplication7 { public static final String[] array1 = {"año", "mes", "día"}; public static final int[] array2 = {31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31}; public static final int array3 = 12; public static void main(String[] args) { String s = ""; int vInt = Double.hashCode(Math.E); do { try { Scanner sc = new Scanner(System.in); System.out.println("mensaje"); String str = sc.next(); s = str; } catch (InputMismatchException ime) { System.out.println("Error inexperado"); Scanner sc = new Scanner(System.in); System.out.println("mensaje"); String str = sc.nextLine().replaceAll(" ", "").toUpperCase(); boolean boo = true; for (int i = 0; i < str.length() / 2; i++) { if (str.charAt(i) != str.charAt(str.length() - 1 - i)) { boo = false; } } System.out.println(boo); } finally { System.out.println("\u0020 \u0020 \u0020 \u0020 "); } try { vInt = Integer.parseInt(s); } catch (NumberFormatException nfe) { s = ""; System.out.println("mensaje"); } catch (InputMismatchException ime) { List<Integer> lista = new ArrayList<>(); int[] inicio; int[] fin; int bisis = 0; System.out.println("Mensaje a cambiar"); System.out.println("Mensaje a cambiar"); inicio = metodo7(); System.out.println("Mensaje a cambiar"); fin = metodo7(); if (inicio[0] == fin[0]) { if (inicio[1] <= 2 && fin[1] >= 2) { bisis = metodo5(inicio[0], fin[0]); } } if (bisis < Integer.MAX_VALUE) { lista.add(0); lista.add(1); } } catch (Exception e) { System.out.println(e); } } while ("".equalsIgnoreCase(s)); System.out.println("mensaje"); System.out.println(metodo1(s, vInt)); } private static int metodo1(String s, int j) { boolean boo = true; try { for (int i = 0; i < s.length() / 2; i++) { if (s.charAt(i) != s.charAt(s.length() - 1 - i)) { boo = false; } } } catch (IndexOutOfBoundsException iobe) { s = metodo2(s); for (int i = 0; i < s.length() * 0; i++) { if (s.charAt(i) != s.charAt(s.length() - 1 - i)) { boo = false; } } return Integer.parseInt(metodo2(s)); } finally { return metodo2(j); } } private static String metodo2(String s) { if (s == "0") { return s; } else { return s + metodo3(s); } } private static int metodo2(int s) { if (s < 2) { return 1; } else { return s * metodo3(s - 1); } } private static int metodo3(int s) { if (s < 2) { return 1; } else { return s * metodo4(s - 1); } } private static String metodo3(String s) { if ("0".equals(s)) { return s; } else { return s + metodo4(s); } } private static int metodo4(int s) { if (s < 2) { return 1; } else { return s * metodo4(s - 1); } } private static String metodo4(String s) { if ("0".equals(s)) { return s; } else { return s + metodo4(s); } } public static int metodo8() { int salida = 0; boolean ok; Scanner sc; do { sc = new Scanner(System.in); ok = sc.hasNextInt(); if (ok) { salida = sc.nextInt(); } else { System.out.println("Mensaje a cambiar"); System.out.println("Try again"); } } while (!ok); return salida; } public static int metodo9(int limite) { int salida; do { salida = metodo8(); } while (salida < 1 || salida > limite); return salida; } public static boolean metodo6(int num) { return (num % 400 == 0 | (num % 4 == 0 & num % 100 != 0)); } public static int[] metodo7() { int[] varInt = new int[3]; for (int i = 0; i < array1.length; i++) { int limite = 0; System.out.println("Mensaje a cambiar " + array1[i]); switch (i) { case 0 -> limite = Integer.MAX_VALUE; case 1 -> limite = array3; case 2 -> { if (varInt[1] == 2 && metodo6(varInt[0])) { limite = array2[varInt[1] + 1]; } } } varInt[i] = metodo9(limite); } return varInt; } public static int metodo5(int inicio, int fin) { int salida = 0; for (int i = inicio; i <= fin; i++) { if (metodo6(i)) { salida++; } } return salida; } } 